package utils;

import android.content.Context;

public class CustomToast_Text {
	public CustomToast_Text(Context context, String text) {
		//CustomToast_Text0.makeText(context, text).show();
		//CustomToast_Text1.makeText(context, text).show();
	}
}